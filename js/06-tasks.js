/*
 * Напиши скрипт перевірки підписки користувача придоступі до контента
 * - Є три типа підписки: free, pro і vip.
 * - Отримати доступ можуть тільки користувачі pro і vip
 */
/* 
const sub = "pro";

const isPro = sub === "pro"; // t
const isVip = sub === "vip"; // f

if (isPro || isVip) {
  console.log("Hello");
} else {
  console.log("Bye");
} */

// ============================

/* const userName = "Yaroslav";
const balance = 50;

const isNotEmptyUserName = userName.length > 0;
const isPositiveBalance = balance > 0;

const isValidData = isNotEmptyUserName && isPositiveBalance;

if (isValidData) {
  console.log("hello");
} */
/*
 * Напиши скрипт, який перевіряє можливість відкрити чат з користувачем.
 * Для цього, користувач має бути:
 * - другом
 * - онлайн
 * - без режима не турбувати
 */

/* const isOnline = true;
const isFriend = true;
const isDnd = false;

const canOpenChat = isOnline && isFriend && !isDnd;

console.log("Можно открыть чат? ", canOpenChat); */
