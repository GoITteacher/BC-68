/*
 * Інтерфейс classList
 * - add
 * - remove
 * - toggle
 * - replace
 * - contains
 */

// const ulElem = document.querySelector('ul.site-nav');

// =======================================================
/* for (let i = 0; i < ulElem.children.length; i += 3) {
  ulElem.children[i].classList.add('first-elem');
}

ulElem.lastElementChild.classList.add('hidden'); */
// =======================================================

// ulElem.firstElementChild.classList.remove('first-elem');

// =======================================================

/* ulElem.addEventListener('click', () => {
  ulElem.classList.toggle('first-elem');
}); */

// =======================================================

/* const firstLiElem = document.querySelector('ul>li');

firstLiElem.classList.remove('upper');
firstLiElem.classList.add('lowerCase');

firstLiElem.classList.replace('upper', 'lowerCase'); */

// =======================================================

// const ulElem = document.querySelector('ul.site-nav');
// ulElem.classList.add('upper');

// if (ulElem.classList.contains('upper')) {
//   console.log('Good');
// } else {
//   console.log('Bad');
// }

// =======================================================

// const ulElem = document.querySelectorAll('ul')[1];

// for (const child of ulElem.children) {
//   if (child.textContent.length >= 5) {
//     child.classList.add('item');
//   }
// }

// =======================================================
